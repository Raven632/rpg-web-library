services:
  rpg-library:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rpg-library-v2
    ports:
      - "80:3000"
    volumes:
      - ./games:/games
    environment:
      - GAMES_DIR=/games
      - NODE_ENV=production
    restart: unless-stopped
