services:
  rpg-library:
    build: ./api
    container_name: rpg-library-v2
    ports:
      - "80:3000"
    volumes:
      - ./games:/games
      - ./api:/app
      - /app/node_modules
    command: npx nodemon server.js
    restart: unless-stopped #
